schema-additions.ts — 4 new tables appended to your existing schema: lineup_scores (live scoring per lineup), alert_deliveries (tracks SMS/email sends to prevent duplicates), notification_preferences (per-user channel and alert type settings), and performance_snapshots (daily per-user lineup vs optimal vs field stats). All with proper indexes.
feature-routes.ts — Full backend for all 5 features: live score CRUD, notification preference upsert, alert delivery via Twilio/SendGrid (stubbed, ready to uncomment), performance computation worker endpoint, track record public endpoint with sport breakdown and accuracy stats, and a /api/content-access endpoint your frontend can query to know exactly what any user can see.
LiveScoreTracker.tsx — New /live-scores page. Shows each saved lineup as a collapsible card with per-player live/projected points, delta vs projection, game status pips, contest rank if a dkEntryId is stored, and an auto-polling refresh every 60 seconds during live slates. Locked for free users with an upsell card.
NotificationPreferences.tsx — New /notifications page. Three-panel settings: delivery channels (email/SMS with phone-number awareness), alert type toggles (injuries, scoring milestones, pre-game reminders), and a timing slider for pre-game alerts. Right sidebar shows recent alerts with per-alert delivery status chips (SMS sent, email failed, etc.).
GatedContent.tsx — Drop-in blur/teaser components: GatedContent (wraps anything with a blur+overlay+CTA), GatedPlayerRows (table rows with blur fade and "N more hidden" banner), GatedLineupCards (shows 1 lineup free, blurs the rest with a count overlay), GatedStat (blurs a single number), GatedOwnershipSection (fades ownership list at row 5), and GatedBadge. Usage examples included as comments at the bottom.
PerformanceDashboard.tsx — New /performance page (paid only). Summary stats across all sports: vs-optimal %, vs-field %, projection accuracy with letter grade. Best slate highlight card. Per-sport breakdown grid. Slate-by-slate history table with trend arrows, accuracy bars, and sortable columns. Empty state with CTA to build lineups.
TrackRecord.tsx — New /track-record page (fully public — this is your marketing page). Hero with top-line stats, sport-by-sport breakdown cards, expandable result rows showing the full optimal lineup with projected vs actual per player, boost hit rate, and salary utilization. Conversion CTA at the bottom. No auth required — this is a lead generation surface.
INTEGRATION.md — Step-by-step guide covering schema migration, Express mounting, router additions, blur/teaser integration snippets for ProOptimizer and OwnershipHeatmap, nav additions, and how to wire performance computation into your nightly analysis worker.